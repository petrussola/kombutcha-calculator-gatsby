import * as React from "react";
import Layout from "../components/Layout";

const Ingredients = () => {
  return (
    <Layout>
      <h1>Ingredients</h1>
    </Layout>
  );
};

export default Ingredients;
